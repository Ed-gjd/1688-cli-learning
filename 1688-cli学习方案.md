# 1688-cli 学习方案(小白 → 专家)

> 面向跨境卖家 / dropshipper 的 1688 寻源 CLI 工具实操方案。
> 6 级阶梯,每级 = 一个真实工作流场景 + 一组命令 + 一个绕不开的坑。
> 广度:覆盖全部命令组(登录 → 寻源 → 比价 → 尽调 → 成交 → 售后监控)。
> 深度:每级挖穿一个跨境选品的判断原理。

---

## 0. 方案概览

- **目标**:从"只会搜关键词"到"能用 1688-cli 跑通寻源流水线 + 供应商尽调 + 订单监控"。
- **前置条件**:
  - Node 20+(本机 v22.23.1 ✅)
  - 已用 `1688 login` 扫码登录自己的 1688 买家账号
  - 会基本 shell(管道、重定向)、愿意读 JSON
- **产出**:每一级都有"实战任务 + 验收标准",最终产出你自己的寻源工作流脚本。
- **贯穿案例**:用一个真实产品线(下面以 **SDR / RTL2832 接收机** 为例)串起全部 6 级,同时可替换成你任何在做的品类(蓝牙耳机、手机支架……)。

**六级总览**

- **L1 把工具跑起来** —— doctor/login/whoami/logout → 挖穿:daemon、profile、风控滑块
- **L2 关键词搜索 + 结构化读数据** —— search → 挖穿:搜索价 vs SKU 价、排序陷阱
- **L3 单品深挖 + 比价 + 找同款** —— offer/compare/similar/image-search → 挖穿:SKU 引流、销量三口径
- **L4 供应商尽调** —— supplier inspect/search/research → 挖穿:认证三角、店龄×复购率×销量
- **L5 沟通与成交** —— seller/inbox/cart/checkout → 挖穿:下单安全闸、询价留痕
- **L6 订单监控 + 多账号 + 自动化** —— order/shipped/stuck/fake-shipped/daemon/profile/debug → 挖穿:虚假发货、锁冲突、风控节奏

---

## 第 1 级 · 小白:把工具跑起来

**目标**:工具能登进你自己的 1688 号,并理解它背后在跑什么。

**核心命令**

```bash
1688 doctor            # 环境自检:Node / Chromium / 锁 / 会话
1688 login             # 终端弹二维码,手机扫码登录
1688 login --headed    # 风控时打开真实浏览器窗口扫码
1688 whoami            # 看登录的是哪个号
1688 logout            # 退出并清本地会话
1688 daemon status     # 看后台守护进程状态
```

**深度知识点**

- **daemon 是什么**:CLI 背后常驻一个 `serve` 进程(浏览器会话跑在它里面),大部分命令都转发给它。`doctor` 里"browser launch OK (bundled Chromium)"说明它自带浏览器,不用另装 Chrome。
- **cookie 落盘**:会话存在 `~/.1688/profiles/<profile>/`,支持 `--profile` 多账号隔离。
- **风控滑块**:连续快速查询会触发 1688 反爬,daemon 自动暂停(`DAEMON_PAUSED`,含 `pausedUntil` 到期时间),这是保护不是 bug,歇够时间或 `--headed` 手动过滑块即可恢复。
- **锁冲突 `LOCK_BUSY`**:一个 profile 同时只允许一个进程独占浏览器目录,`login` 和 daemon 撞上就报这个。先 `1688 daemon stop` 再 login。

**实战任务**:走一遍 `doctor → login → whoami`,再故意 `daemon start` 后跑一次 `login` 复现 `LOCK_BUSY`,学会用 `daemon stop` 解锁。

**验收标准**:`1688 whoami` 返回 `{"loggedIn":true, "memberId":"...","nick":"..."}`。

---

## 第 2 级 · 入门:关键词搜索 + 结构化读数据

**目标**:会搜、会过滤、能把结果导出成结构化数据落盘。

**核心命令**

```bash
1688 search "蓝牙耳机" \
  --max 20 \
  --sort price-asc \          # relevance | best-selling | price-asc | price-desc
  --price-min 30 --price-max 200 \
  --province 广东 --city 深圳市 \
  --verified factory \        # any | factory | business | super-factory
  --exclude-ads \
  --json --pretty
```

**输出控制(重点)**:`--json` / `--pretty` 给人类看;`--get <dot-path>` 取单字段;`--pick <paths>` 取多字段;管道给 `python3`/`jq` 落盘。

```bash
1688 search "RTL2832" --max 5 --json \
  | python3 -c 'import sys,json; [print(o["price"]["min"], o["title"], o["url"]) for o in json.load(sys.stdin)["offers"]]'
```

**深度知识点**

- **搜索价 ≠ 要买价**:搜索返回的 `price.min` 是**最低规格价**,不是标题宣称的料号价。搜 "RTL2832" 时 ¥70 其实是 FC0012 调谐器,R820T2 那款是 ¥120。
- **排序陷阱**:`--sort price-asc` 会把裸芯片(¥0.06~¥1 的 IC 散片)顶到最前,成品被淹没。要配合 `--price-min` 过滤。
- **过滤字段**:`--verified factory` 筛实力工厂,`--exclude-ads` 去 P4P 广告位,`--province/--city` 锁产地。

**实战任务**:对同一关键词分别跑 `relevance` 和 `price-asc` 两次,对比前 5 条,亲手看排序怎么把裸料顶上来。

**验收标准**:一条命令拉 20 条结果,并成功抽出 `title/price/url` 三列落盘。

---

## 第 3 级 · 进阶:单品深挖 + 比价 + 找同款

**目标**:拿到一条商品后,能看清它真实卖什么、多少钱、走量如何,并横向找同款。

**核心命令**

```bash
1688 offer 592617393291                 # 单品详情:skus/priceTiers/saledCount/attributes
1688 compare 592617393291 965765383603  # 多 offer 横向比价
1688 similar 592617393291               # 找同款,按价排序
1688 image-search ./product.jpg         # 以图搜图(本地文件或 http(s) URL)
```

**深度知识点**

- **`skus` 数组才是真相**:看 `specs/price/stock`,能识破"标题好料号 + 最低规格价"引流。`priceTiers` 有时会被工具错误地摊成"数量阶梯",不可全信。
- **销量三口径打架**:`saledCount`(近期真实销量)、页面"已售 N 台"(近 30 天)、搜索列表 `turnover`(累计/成交额)——同一商品能出 19 / 70+ / 145 三个数。**取最保守的 `saledCount`**。
- **工具字段可能解析错**:`minOrderQty` / `mixOrderQty` 与页面"2 台起批 / 20 件混批"对不上,下单前务必回页面核对。
- **`similar` 的价值**:以一款为锚点拉同款比价,能快速看出这条产品线的价格带和供应商密度。

**实战任务**:`offer` 拉 RTL2832 的 5 个链接,把每家 `skus` 里的调谐器型号(R820T vs R820T2)挑出来,验证"越便宜越要看清料号"。

**验收标准**:能对一条 offer 输出"真实 SKU 价 + 起批量 + 真实销量"三要素。

---

## 第 4 级 · 进阶:供应商尽调

**目标**:下单前能给一家供应商打"小单可试 / 大额慎入"的结论。

**核心命令**

```bash
1688 supplier inspect 592617393291       # 从 offerId 或 b2b-* memberId 拉信任信号
1688 supplier search "蓝牙耳机" 深圳      # 公司搜索
1688 supplier research "蓝牙耳机" 深圳    # 公司搜索 + 打分 + inspect 增强
1688 seller-history <seller>            # 该供应商的历史订单、平均发货天数、准时率
```

**深度知识点**

- **认证信号三角**:`chtMember`(诚信通)、`slsj`/`industrySeller`(实力商家)、`superFactory`(超级工厂)。隆奇通三项全否;亲民是实力商家。
- **`isFactory` vs `isFactoryDealer`**:前者是自报"我是工厂",后者是认证工厂直销,两者差别很大,别看到 `isFactory:true` 就信。
- **店龄 × 复购率 × 真实销量 交叉判断**:老店但零认证(隆奇通 10 年)≠ 可信;新店但实力商家 + 销量高(亲民 3 年 + 215)要分开看——亲民非跨境专供、不支持代发。
- **局限**:`shopcardCaptured:false` 时回头率/服务评分/诚信年限抓不到,用 `--headed` 重试补全。

**实战任务**:对 5 家 SDR 供应商都跑 `supplier inspect`,整理出一张"认证 / 店龄 / 销量 / 是否代发 / 是否跨境专供"对照,给出每家的下单建议。

**验收标准**:能输出一份供应商评估结论,而不是只贴原始 JSON。

---

## 第 5 级 · 熟练:沟通与成交(询价 → 下单)

**目标**:会询价留痕、加购、走到下单前一步(但不真付)。

**核心命令**

```bash
1688 inbox                       # 最近旺旺会话
1688 seller inquire <offerId>    # 对某商品发起询价
1688 seller chat <seller>        # 旺旺沟通
1688 seller messages             # 消息列表
1688 cart add <offerId> --sku <skuId> --qty 2
1688 cart list
1688 checkout prepare            # 预览下单(运费/总额/收货地址)
1688 checkout confirm            # 确认下单(受 TTY / --agent 限制)
```

**深度知识点**

- **下单安全闸**:`checkout confirm` 受 TTY 或 `--agent` 标志限制,防止 agent 静默付款。这是工具刻意设计的安全边界,自动化脚本里尤其要懂——没有这个闸,agent 可能半夜替你下单。
- **询价留痕**:`inbox` / `seller messages` 是旺旺沟通留痕,对售后、纠纷、对账都有用。
- **采购车 vs 直接下单**:`cart add` 走采购车适合多店混批凑单;`checkout` 是直接结算。

**实战任务**:对某商品走一遍 `cart add → cart list → checkout prepare`,停在 prepare(不 confirm),看懂运费和总额构成。

**验收标准**:能解释 `checkout confirm` 为什么需要额外确认,并知道 `--agent` 标志的用途。

---

## 第 6 级 · 专家:订单监控 + 多账号 + 自动化编排

**目标**:把 1688-cli 接进脚本或 Claude Code,做自动化的寻源 + 售后监控流水线。

**核心命令**

```bash
1688 order list / get <id> / logistics <id>
1688 shipped <orderId>          # 订单详情 + 物流合看
1688 stuck                      # 付款后 N 天未发货(默认 3 天)
1688 fake-shipped               # 已发货但物流冻结在"等待揽收"(疑似虚假发货)
1688 seller-history <seller>    # 供应商画像:平均发货天数 + 准时率
1688 daemon start/stop/reload/status
1688 profile list / status      # 多账号隔离
1688 debug list / last / show   # 失败诊断(看最近事件和失败产物)
```

**深度知识点**

- **`stuck` / `fake-shipped` 是跨境卖家最实用的售后信号**:能自动揪出"假发货"供应商,这是人工翻订单很难盯全的。
- **多账号 + 锁**:不同 profile 并行互不串,但一个 profile 同时只一个进程(我们撞过 `LOCK_BUSY`),自动化里要处理锁与排队。
- **风控节奏**:自动化要留退路——限流、daemon 暂停(`pausedUntil`)、`--headed` 手动过滑块,别把号跑死。
- **`--json-v2`**:给 agent 消费的响应封装(带 ok/code/message),接 Claude Code 时用它做结构化返回。
- **`debug`**:失败时先 `debug last` 看请求事件和产物,再决定重试还是 `--headed`。

**实战任务**:写一个脚本(或一段 Claude Code 提示),每天自动跑 `stuck + fake-shipped + seller-history`,输出"本周异常订单 + 供应商准时率排行"。

**验收标准**:一条命令(或一个脚本)产出可读的监控报告,而非一堆裸 JSON。

---

## 7. 贯穿实战案例(可直接替换)

以 **SDR / RTL2832 接收机** 为贯穿案例,6 级一路跑下来会得到:

- L2:关键词 `RTL2832` 搜索,发现价格带 ¥70~1197
- L3:`offer` 拆出各家 SKU,发现 R820T vs R820T2、FC0012 的调谐器差异
- L4:`supplier inspect` 五家横评,识别"隆奇通=老店零认证 / 亲民=实力商家有量"
- L5:对目标商品询价 + 加购,停在 prepare
- L6:写监控脚本盯假发货

换成你自己的品类(蓝牙耳机、手机支架……)时,命令不变,只换关键词和 offerId。

---

## 8. 节奏建议

- **快速过一遍**:每级 1 个实战任务,半天到一天走完 6 级,先把工具摸顺。
- **精练**:L3 + L4(选品 + 尽调)是跨境卖家价值最高、最该花时间的两级,建议各练 2~3 个真实品类。
- **进阶方向**:L6 之后,把常用流程固化成你自己的脚本/Claude Code 提示,沉淀成"寻源 SOP"。

## 9. 护栏与风险(务必遵守)

- **下单前人工确认**:`checkout confirm` 属于真实资金动作,自动化里永远留人工闸。
- **风控别硬怼**:遇到 `DAEMON_PAUSED` / 滑块,等或 `--headed`,不要高频重试把号跑死。
- **字段不可全信**:工具的 `minOrderQty` / `mixOrderQty` / `priceTiers` 偶有解析错,重大决策回页面核对。
- **账号安全**:cookie 存本机 `~/.1688/`,别把 `profiles/` 目录提交到 git 或外传。
- **合规**:工具驱动的是你自己真实登录的买家会话,只做你手动会做的操作,不用于批量爬取。
